import { AboutProps } from '@/types';
import React from 'react';

export default function About(aboutProps: AboutProps) {
  return (
    <section className="h-screen bg-slate-500 pt-20" id="about">
      <div>About</div>
    </section>
  );
}
